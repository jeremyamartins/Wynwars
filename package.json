{
  "dependencies": {
    "dotenv": "^8.0.0",
    "express": "^4.16.4",
    "path": "^0.12.7"
  },
  "scripts": {
    "start":"node server.js",
    "heroku-postbuild": "cd client && yarn && yarn run build"
  }
}
